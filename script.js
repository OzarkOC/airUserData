import { retrieveData } from "./scripts/userData";

window.Webflow ||= [];
window.Webflow.push(() => {
  // ATTRIBUTES
  const userEmail = document.querySelector(".userdata").getAttribute("uEmail");
  console.log(userEmail);
  // MARKUPS
});
